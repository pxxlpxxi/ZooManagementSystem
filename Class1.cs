using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace ZooManagementSystem
{
    internal class Class1
    {
        //DateTime birthdate = new DateTime(2020, 08, 23);
        //DateTime today = DateTime.Now;
        //public Class1()
        //{

        //}
        //public (int age, string bday) GetAge()
        //{
        //    DateTime today = DateTime.Now;
        //    int age = today.Year - birthdate.Year;

        //    //if today is smaller than birthday+age aka. if thís years
        //    //birthday hasnt passed yet, subtract one year from age
        //    if (today < birthdate.AddYears(age))
        //    { age--; }

        //    string bday = birthdate.ToString("MMMM dd yyyy", new CultureInfo("en-US"));
        //    return (age, bday);
        //}
    }
}
